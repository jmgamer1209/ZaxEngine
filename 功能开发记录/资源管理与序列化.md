资源管理开发分三个阶段：
1. 基础资源导入器，拆分成多个文件，同时，需要实现 YAML 序列化，因为 mat 和 mesh 等，都需要序列化支持
2. 实现 Library 库，直接以二进制形式存储，运行时可以直接加载二进制文件，而不是运行时再转化
3. 对 FBX 等文件，保留源文件

### 基础资源导入器
资源导入，并不是只是一个文件管理器，还需要拆分资源，对于资源进行序列化保存，以及加载。
此功能初始开发引入版本为 v0.1.20